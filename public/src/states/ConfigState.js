// Strings.
import strings from '../config/strings.json';

export default {
    isDrawerOpen: false,
    isLoading: false,
    pageTitle: strings.APP_TITLE,
    snackBar: {
        isOpen: false,
        message: ''
    }
};
