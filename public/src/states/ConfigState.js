// Strings.
import strings from '../config/strings.json';

export default {
    isDrawerOpen: false,
    isLoading: false,
    pageTitle: strings.APP_TITLE,
    sessionToken: null,
    snackBar: {
        isOpen: false,
        message: ''
    }
};
